name: Deprecated — Latest Blog Posts

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deprecation
        run: echo "Disabled because the profile no longer injects blog posts into the README."
