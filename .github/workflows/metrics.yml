name: Deprecated — GitHub Metrics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deprecation
        run: echo "Disabled because the profile no longer publishes generated activity metrics."
